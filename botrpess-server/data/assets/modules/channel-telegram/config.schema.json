{
  "type": "object",
  "properties": {
    "botToken": {
      "description": "The bot token received from the Telegram Botfather",
      "type": "string"
    },
    "enabled": {
      "description": "Enable or disable this channel for this bot",
      "type": "boolean"
    },
    "forceWebhook": {
      "description": "Force usage of webhooks",
      "type": "boolean"
    },
    "chatUserAuthDuration": {
      "description": "The duration of the authentication session when a user authenticate through this channel.",
      "default": "24h",
      "type": "string"
    },
    "$schema": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "botToken",
    "chatUserAuthDuration",
    "enabled",
    "forceWebhook"
  ],
  "$schema": "http://json-schema.org/draft-07/schema#"
}

